import React from "react";
import imageTempData from "../temp";
import { storage } from "../../config/firebaseConfig";

const HandleUploadImage = (image, onSuccess, onFailure, onProgess) => {
  if (image) {
    
  }
};
export default HandleUploadImage;
